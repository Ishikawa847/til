# アプリ開発
onChangeについて
 Event ハンドラ関数。制御された入力の場合は必須。ユーザが入力の値を変更するとすぐに発火します（例えば、キーストロークごとに発火します）。ブラウザの input イベントと同じように動作します。
 例：
<input
type="text"
value={name}
onChange={nameChange}
placeholder="name"
/>
値が入力されるとnameChange関数が実行され